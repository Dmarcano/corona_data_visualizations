{
  "name": "corvis",
  "version": "1.0.0",
  "description": "Visualization of Corona epidemic",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "author": "Diego Marcano",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "node-fetch": "^2.6.0",
    "node-json-db": "^1.0.3",
    "papaparse": "^5.1.1"
  },
  "devDependencies": {}
}
